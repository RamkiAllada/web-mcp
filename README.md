# web-mcp
What WebMCP Does (Frontend) Browser-based standard to expose UI capabilities to agents Lets agents interact with your live website Works only when: Page is open User is active in browser tab
